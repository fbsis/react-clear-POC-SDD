# Visual review gates

Each visual gate captures deterministic fixtures at 375x812, 768x1024 and 1440x900. A gate remains
open until the product owner records approval. Requested adjustments replace the screenshots and add a
new dated decision entry; automated screenshot generation alone never completes a gate.

Screenshots live in `screenshots/` and use `<surface>-<viewport>.png` names.
